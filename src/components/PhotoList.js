import React from "react";
import { View, Text, StyleSheet } from "react-native";

const PhotoList = ({}) => {
  return (
    <View>
      <Text>Photo list component</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PhotoList;
